table! {
    tweets (id) {
        id -> Int4,
        tweet_id -> Varchar,
        text -> Text,
        tweet_link -> Varchar,
        user_link -> Varchar,
        tweet_time -> Varchar,
        user_name -> Varchar,
        screen_name -> Varchar,
        profile_image_url -> Varchar,
    }
}
