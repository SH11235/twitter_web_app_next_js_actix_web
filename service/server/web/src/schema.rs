table! {
    tweets (id) {
        id -> Int4,
        text -> Text,
        tweetlink -> Varchar,
        userlink -> Varchar,
        tweettime -> Varchar,
        username -> Varchar,
        screenname -> Varchar,
        profileimageurl -> Varchar,
    }
}
